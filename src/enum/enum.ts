export enum TYPE {
  SIGNUP = "signup",
  SECESSION = "secession",
  LOGIN = "login",
  COUNTRY = "country",
}

export enum AVGIF {
  Y = "y",
  M = "M",
  D = "d",
}

export enum TYPEIF {
  COUNTRYANALYTICS = "CountryAnalytics",
}